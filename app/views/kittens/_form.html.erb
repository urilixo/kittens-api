<%= form_with(model: kitten) do |form| %>
  <% if kitten.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kitten.errors.count, "error") %> prohibited this kitten from being saved:</h2>

      <ul>
        <% kitten.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.label :Kitten_name %>
    <%= form.text_field :name %><br>
    <%= form.label :age %>
    <%= form.select :age, (1..12).map{ |i| i == 1 ? ["#{i} month old", i] : ["#{i} months old", i] } %><br>
    <%= form.label :cuteness %>
    <%= form.text_field :cuteness %><br>
    <%= form.label :softness %>
    <%= form.text_field :softness %><br>
    <%= form.submit %><br>
  </div>
<% end %>
